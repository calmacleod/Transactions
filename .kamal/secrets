# Secrets defined here are available for env/secret in config/deploy.yml.
# Keep this file as dotenv-compatible assignments only.
# Required local environment:
#   BWS_ACCESS_TOKEN=<Bitwarden Secrets Manager access token>

SECRETS=$(kamal secrets fetch --adapter bitwarden-sm e52183e9-3a50-41d7-a400-b455012360d2/all)

KAMAL_SERVER_IP=$(kamal secrets extract KAMAL_SERVER_IP ${SECRETS})
KAMAL_APP_HOST=$(kamal secrets extract KAMAL_APP_HOST ${SECRETS})
RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
ADMIN_EMAIL=$(kamal secrets extract ADMIN_EMAIL ${SECRETS})
ADMIN_PASSWORD=$(kamal secrets extract ADMIN_PASSWORD ${SECRETS})
OPENAI_API_KEY=$(kamal secrets extract OPENAI_API_KEY ${SECRETS})
RESEND_API_KEY=$(kamal secrets extract RESEND_API_KEY ${SECRETS})
MAILER_FROM=$(kamal secrets extract MAILER_FROM ${SECRETS})
